# NUMERA Frontend

React + TypeScript + Vite UI for the NUMERA Personal Decision Intelligence platform.

## Scripts

```bash
npm install
npm run dev      # http://localhost:5173
npm run build
npm run preview
```

API calls use the Vite proxy (`/api` → `http://127.0.0.1:8000`).
